call by value#include <stdio.h>
#include<stdlib.h>
void swap( int x, int y)
{
    int t;
    t=x;
    x=y;
    y=t;
}

int main()
{
  int a,b;
  printf("enter two numbers");
  scanf("%d%d",&a,&b);
  printf("values before swpping a=%d b=%d\n",a,b);
  swap(a,b);
  printf("values ");
  return 0; 
}
it is not change any values 
output: a=45 b=67
  a=45 b=67
